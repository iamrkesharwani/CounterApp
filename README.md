# Counter App

A simple React counter app to practice state management using `useState`.

### Features
- Increase, decrease & reset counter  
- Value never goes below zero  
- Clean UI with Tailwind CSS  

### Run locally
```bash
npm install
npm run dev
```
